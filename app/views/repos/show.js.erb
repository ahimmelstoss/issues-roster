// on click of the repo name, append rendered partial that is a list of issues
// toggle down
// on click again of repo name, hide that partial
// toggle up

$('#holderDiv').html('<%= escape_javascript(render :partial => "repo") %>').slideToggle("slow");