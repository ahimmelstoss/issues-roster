<h1>Organizations</h1>

<% @orgs_repos.each do |org_repo| %>
  <h4><%= org_repo[:org_name] %></h4>
    <% org_repo[:org_repos].each do |repo| %>
    <a href="https://github.com/<%= org_repo[:org_name] %>/<%= repo %>"><%= repo %></a><br>
    <% end %>
<% end %>
